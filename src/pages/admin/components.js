import AddButton from '@/admin_components/addButton'
import React from 'react'

function Components() {
  return (
    <div>
        <AddButton onClick={() => {}}>Add Place</AddButton>
    </div>
  )
}

export default Components